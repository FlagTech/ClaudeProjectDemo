# CLAUDE.md

## 專案說明

Focus Board 是一個純前端的輕量待辦事項網頁，使用 HTML5、CSS3 與原生 JavaScript 實作，不依賴任何後端服務、外部 API 或建置工具。所有待辦資料儲存在瀏覽器的 `localStorage`，重新整理或重新開啟瀏覽器後仍會保留。

本專案同時是「Claude Code 專案遷移至 OpenCode」的教學範例，因此部分功能刻意保留給 OpenCode 階段接手，詳見 `docs/PRD.md` 第 7 節與 README 的「尚未完成項目」。

## 開發規則

* 不使用 React、Vue、Svelte 等前端框架。
* 不加入 npm、Vite、Webpack 等建置工具。
* 不加入外部套件依賴。
* 資料必須儲存在 `localStorage`。
* HTML、CSS、JavaScript 必須分離存放於 `src/index.html`、`src/style.css`、`src/app.js`。
* 修改時必須維持既有視覺風格（簡潔、留白、按鈕與輸入欄位風格一致）。
* 完成修改後，需在回覆中說明修改了哪些檔案，以及如何測試。
* 不得實作 `docs/PRD.md` 第 7 節列出的預留功能（工作篩選、清除已完成項目、篩選狀態樣式）。
* 不要建立 `AGENTS.md`，該檔案將由後續 OpenCode 遷移教學建立。

## 目前狀態

Claude Code 階段已完成：

* 新增待辦事項（按鈕或 Enter），空白內容不會被新增，前後空白自動移除
* 顯示待辦清單，每筆項目含完成狀態控制、內容、刪除按鈕
* 切換完成／未完成狀態，已完成項目具備清楚的視覺差異
* 刪除單一待辦事項
* 工作統計（全部／未完成／已完成）即時更新
* 無項目時顯示空白提示
* localStorage 資料持久化，並處理無資料與資料損壞的情況
* 基本無障礙支援與響應式版面

## 尚未完成

以下功能預留給 OpenCode 階段實作：

* 全部／待完成／已完成三種篩選模式
* 「清除已完成」按鈕，一次刪除所有完成項目
* 目前選取篩選模式的視覺狀態樣式

畫面中已預留可放置上述功能的區域（統計區與工作清單附近），但目前不包含任何可操作的篩選或清除按鈕。
